# <Project Title>

> One-sentence description of what this project does and the skill it demonstrates.

**Category:** <category> · **Skills:** <comma-separated>

## Problem

What real problem in AI training/evaluation this addresses, in two or three sentences.

## Approach

How it works. Keep it concrete: the core algorithm/metric, the data, the interfaces.

## How to run

```bash
python -m pytest            # deterministic core, no API key needed
python run.py --help        # entry point
```

Set `ANTHROPIC_API_KEY` to run against a live model. Without it, the mock provider
and committed sample data are used.

## Sample output

Paste or link a small, real example from `results/`.

## What this demonstrates

Two or three bullets naming the concrete skills a reviewer should take away.
